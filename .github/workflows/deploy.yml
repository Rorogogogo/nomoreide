name: Release (Version Bump & Publish to npm)

# Releasing is deliberate, not a side effect of merging. Merges accumulate on
# main — tested by ci.yml on every push — and a release is cut by running this
# workflow. Tagging v<version> here is what triggers desktop-release.yml.
on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Version bump"
        type: choice
        options: [patch, minor, major]
        default: patch

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.REPO_PAT }}

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
          package-manager-cache: false

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Run tests
        run: npm test

      - name: Bump version and tag
        env:
          REPO_PAT: ${{ secrets.REPO_PAT }}
          BUMP: ${{ inputs.bump }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${REPO_PAT}@github.com/Rorogogogo/nomoreide.git"
          npm version "${BUMP:-patch}" --no-git-tag-version
          NEW_VERSION=$(node -p "require('./package.json').version")
          node scripts/sync-version.mjs "$NEW_VERSION"
          echo "NEW_VERSION=$NEW_VERSION" >> "$GITHUB_ENV"
          git add package.json package-lock.json apps/dashboard/package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml src-tauri/Cargo.lock
          git commit -m "chore: bump version to $NEW_VERSION"
          git tag "v$NEW_VERSION"
          git push
          git push --tags

      - name: Publish to npm
        run: npm publish --access public
