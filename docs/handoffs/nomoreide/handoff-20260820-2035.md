# Rust runtime migration — Phase 1 continuation

## Goal / current focus

Continue the approved Rust runtime migration on branch `rust-refactoring`. Repository cleanup and the first Phase 1 workspace slice are complete. Start with the next Phase 1 item: introduce an event-sink abstraction in `nomoreide-core` and route existing Tauri `app.emit` sites through it without changing behavior.

## State

- Clean local branch `rust-refactoring`; nothing has been pushed.
- `777db38 test(mcp): freeze compatibility contract` completed PR-sequence item 1.
- `5f5299f refactor(repo): extract dashboard workspace`, `92907b2 refactor(repo): extract website workspace`, and `7f4cd5e refactor(repo): organize project documents` completed repository cleanup.
- `95ae7aa refactor(rust): establish cargo workspace` created the six-crate workspace, moved `src-tauri` to `crates/nomoreide-tauri`, and lifted its reusable core into `crates/nomoreide-core`.
- Placeholder boundaries exist for `nomoreide-daemon`, `nomoreide-daemon-client`, `nomoreide-mcp`, and `nomoreide-cli`.
- CI/release/version paths use the root Cargo workspace. `scripts/check-rust-dependencies.mjs` rejects direct or transitive `nomoreide-daemon-client` dependencies on `nomoreide-core`.
- Verification passed: Rust fmt, warning-free Clippy, 100 Rust tests, Tauri debug build, root build, 1,780 Vitest tests, lint, website build, version sync, dependency guard, and npm pack dry run.
- No work is currently in progress.

## Next work

1. Implement the event-sink abstraction in `nomoreide-core` as a focused local commit.
2. Locate every existing Tauri `app.emit` call and route core-originated events through a Tauri adapter; preserve event names, payloads, ordering, and threading behavior.
3. Keep Tauri as the only event-sink implementation for now and add focused tests for the abstraction.
4. Gate with Rust fmt, Clippy with warnings denied, workspace tests, and a Tauri debug build; run broader Node gates if active shared paths change.
5. Continue Phase 1 afterward: config/state security gaps, daemon discovery/lock/auth, then native MCP initialization and exact 90-tool listing.

## Key decisions and constraints

- Local commits only until the entire Rust refactoring is finished. Do not push, create a PR, or mutate remotes.
- Preserve parallel writers' work. Never stash; inspect the tree before edits and keep slices short-lived and separate.
- The daemon-client crate name is resolved as `nomoreide-daemon-client`.
- `nomoreide-daemon-client` must never depend directly or transitively on `nomoreide-core`.
- Keep `dist/web/client` unchanged. Keep `src/` and `test/` until the TypeScript runtime is removed in Phase 8.
- Counterpart does not imply parity; all ports are judged by the frozen item-1 MCP harness.
- The lifted Tauri core is behavior-preserving, but the Tauri shell still constructs core state. Full daemon ownership convergence remains future Phase 1/6 work.
- Three macOS Unix-socket tests require permission to bind under `/tmp` in the Codex sandbox; they pass when `cargo test --workspace` is run with the required permission.
- Planning documents retain historical source-path descriptions where they explain the migration. Active consumers no longer reference `src-tauri`.

## Open questions / blockers

- None for the event-sink slice.

## Pointers

- Migration source of truth: `docs/plans/2026-08-20-native-rust-runtime-and-mcp.md`
- Layout plan: `docs/plans/2026-08-20-repo-layout-cleanup.md`
- Workspace manifest: `Cargo.toml`
- Shared Rust core: `crates/nomoreide-core/src/lib.rs`
- Tauri shell and commands: `crates/nomoreide-tauri/src/`
- Dependency boundary guard: `scripts/check-rust-dependencies.mjs`
- Frozen MCP contract: `test/fixtures/mcp-tool-contract.json`
- CI gates: `.github/workflows/ci.yml`

## Suggested skills

- `feature-dev` for the event-sink slice and subsequent Phase 1 vertical slices.
- `verification` for full-story gates before each local commit.
- Defer `code-review` until a PR is eventually authorized; perform local diff review meanwhile.
