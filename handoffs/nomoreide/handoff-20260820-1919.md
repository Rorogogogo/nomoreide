# Rust runtime migration — repository cleanup continuation

## Goal / current focus

Continue the approved Rust runtime migration on branch `rust-refactoring`. PR-sequence item 1 is complete. Continue item 2, repository layout cleanup, starting with moving `website` to `apps/website`.

## State

- Clean local branch `rust-refactoring`; nothing has been pushed.
- `777db38 test(mcp): freeze compatibility contract` completes item 1: the 90-tool MCP snapshot/parity harness, drift checks, and tests.
- `5f5299f refactor(repo): extract dashboard workspace` completes layout-cleanup step 1: `src/web/client` moved to `apps/dashboard`, manifests/configs split, release paths updated, and `dist/web/client` preserved.
- Dashboard extraction passed `npm ci`, dashboard typecheck, build, all 1,780 tests, lint, version check, npm pack/install smoke testing, website build, and an isolated daemon HTTP/static-assets smoke test.
- No work is currently in progress.

## Next work

1. Move `website` to `apps/website` as a separate local commit.
2. Make it a workspace sibling depending on `@nomoreide/dashboard`.
3. Remove the website's 35-entry alias bridge, `dedupe` list, and `scripts/verify-shared-deps.mjs`.
4. Update every active path consumer, including CI/release/version-sync scripts.
5. Gate with `npm --prefix apps/website run build` and verify embedded `WorkbenchApp` uses one React instance.
6. Then perform the pure document moves described as cleanup step 3 in another local commit.
7. Only after repository cleanup is complete, begin PR-sequence item 3 / Phase 1: Cargo workspace, `src-tauri` to `crates/nomoreide-tauri`, and the `core/` lift.

## Key decisions and constraints

- The user explicitly requires **local commits only until the entire Rust refactoring is finished**. Do not push, create a PR, or mutate remotes.
- Preserve parallel writers' work. Never stash; inspect the tree before edits and keep layout commits short-lived and separate.
- Keep `dist/web/client` unchanged; it is a runtime contract.
- Keep `src/` and `test/` where they are; the TypeScript runtime is removed only in Phase 8.
- `nomoreide-client` must never depend on `nomoreide-core` when the Cargo workspace is created.
- Counterpart does not imply parity; all later ports are judged by the frozen item-1 harness.
- The plan documents still contain their original planning-status wording. The user explicitly approved implementation in this conversation; do not treat that stale header as a blocker.

## Open questions / blockers

- None for the website move. Use existing workspace conventions from `apps/dashboard`.
- The `nomoreide-client` versus `nomoreide-daemon-client` naming question remains open for Phase 1; it does not block repository cleanup.

## Pointers

- Migration source of truth: `docs/plans/2026-08-20-native-rust-runtime-and-mcp.md`
- Layout source of truth: `docs/plans/2026-08-20-repo-layout-cleanup.md`
- Dashboard workspace: `apps/dashboard/package.json`
- Root workspace manifest: `package.json`
- Version synchronization: `scripts/sync-version.mjs`
- CI/release workflows: `.github/workflows/ci.yml`, `.github/workflows/deploy.yml`, `.github/workflows/desktop-release.yml`

## Suggested skills

- `feature-dev` for the website workspace extraction.
- `verification` for the build and one-React-instance gate.
- `code-review` only when a PR eventually exists; the current no-push constraint means local review is required for now.
