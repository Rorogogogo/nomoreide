# NoMoreIDE Full AI Documentation

NoMoreIDE is an AI-native local development workbench for services, logs, Git review, GitHub PR/issues/Actions workflows, database inspection, SQL review, terminal access, and MCP workflows. It runs locally and gives humans and AI coding agents one shared control surface.

Human docs: https://www.nomoreide.com/docs
AI agent guide: https://www.nomoreide.com/docs/ai-guide.md
GitHub: https://github.com/Rorogogogo/nomoreide
npm: https://www.npmjs.com/package/nomoreide

## Quick Start

Run directly:

```bash
npx -y nomoreide
```

Install globally:

```bash
npm install -g nomoreide
```

Build from source:

```bash
git clone https://github.com/Rorogogogo/nomoreide.git
cd nomoreide
npm install
npm run build
```

## MCP Setup

Claude Code:

```bash
claude mcp add --transport stdio nomoreide -- npx -y nomoreide
```

Codex CLI:

```bash
codex mcp add nomoreide -- npx -y nomoreide
```

Gemini CLI:

```json
{
  "mcpServers": {
    "nomoreide": {
      "command": "npx",
      "args": ["-y", "nomoreide"]
    }
  }
}
```

Universal prompt:

```text
Please set up NoMoreIDE as a local MCP server for this agent. Register a server named nomoreide that runs npx -y nomoreide. After adding it, tell me how to verify it with /mcp, then open the Web UI at http://127.0.0.1:4317/.
```

Verify with `/mcp`.

## Interfaces

Default MCP server:

```bash
nomoreide
```

Terminal UI:

```bash
nomoreide tui
```

Web dashboard:

```bash
nomoreide web
```

Default dashboard URL:

```text
http://127.0.0.1:4317
```

## CLI Reference

Important commands:

```bash
nomoreide setup
nomoreide web
nomoreide tui
nomoreide list
nomoreide add service backend --command "npm run dev" --cwd /absolute/path/to/backend --port 3001
nomoreide add bundle full-stack db backend frontend
nomoreide start backend
nomoreide stop backend
nomoreide restart backend
nomoreide logs backend
```

Git commands:

```bash
nomoreide git status --cwd /path/to/repo
nomoreide git diff --cwd /path/to/repo
nomoreide git stage --cwd /path/to/repo src/index.ts
nomoreide git unstage --cwd /path/to/repo src/index.ts
nomoreide git commit --cwd /path/to/repo --message "feat: add dashboard"
nomoreide git log --cwd /path/to/repo
nomoreide git branch --cwd /path/to/repo
nomoreide git fetch --cwd /path/to/repo
nomoreide git switch --cwd /path/to/repo feature/my-work
nomoreide git create-branch --cwd /path/to/repo feature/new-work
nomoreide git add-repo app --path /path/to/repo
nomoreide git select-repo app
```

## Web Dashboard Guide

Services: register, start, stop, restart, inspect process state, inspect ports, open URLs, and manage bundles.

Logs: read recent stdout and stderr, inspect service context, and use logs for debugging.

Git Review: select repositories, inspect status and diffs, stage explicit files, unstage files, commit staged changes, and inspect commit CI status.

GitHub: connect with a token or OAuth device flow, review PRs, issues, comments, CI status, and GitHub Actions workflow runs. Create PRs/issues or merge PRs only when user intent is explicit.

Workflows: run built-in commit/push/ship templates, author reusable workflows from action steps, agent steps, and human approval gates, and keep blocked/failed run status visible.

Error Inbox: review deduplicated incidents and generate debugging prompts.

Database: list registered connections, list tables, sample rows, inspect schema metadata, run read-only SQL, generate SQL with the agent dock, and use unlock + preview + commit for human-approved writes.

Terminal: work close to the local dashboard context.

Agent dock: pass structured UI context to the connected AI agent; inspect MCP servers, skills, plugins, hooks, usage, and tool-call activity.

## MCP Tool Reference

Service tools:

```text
nomoreide_list_services
nomoreide_register_service
nomoreide_start_service
nomoreide_stop_service
nomoreide_restart_service
nomoreide_read_logs
nomoreide_register_bundle
nomoreide_start_bundle
nomoreide_stop_bundle
nomoreide_status
nomoreide_service_context
nomoreide_service_health
nomoreide_timeline
```

Repo onboarding:

```text
nomoreide_onboard_repo
```

Git tools:

```text
nomoreide_git_status
nomoreide_git_branches
nomoreide_git_switch_branch
nomoreide_git_create_branch
nomoreide_git_fetch
nomoreide_git_diff
nomoreide_git_staged_diff
nomoreide_git_log
nomoreide_git_stage
nomoreide_git_unstage
nomoreide_git_commit
nomoreide_git_register_repository
nomoreide_git_select_repository
```

Error tools:

```text
nomoreide_list_errors
nomoreide_error_prompt
```

Database tools:

```text
nomoreide_list_databases
nomoreide_db_tables
nomoreide_db_sample
nomoreide_db_query
```

GitHub tools:

```text
nomoreide_github_set_token
nomoreide_github_list_prs
nomoreide_github_get_pr
nomoreide_github_get_pr_diff
nomoreide_github_create_pr
nomoreide_github_merge_pr
nomoreide_github_list_issues
nomoreide_github_get_issue
nomoreide_github_list_issue_comments
nomoreide_github_add_issue_comment
nomoreide_github_create_issue
nomoreide_github_get_commit_ci
nomoreide_github_list_workflow_runs
```

Agent and UI tools:

```text
nomoreide_open_ui
nomoreide_close_ui
```

## Configuration Model

Config path:

```text
~/.config/nomoreide/config.json
```

Config sections:

- services: local, Docker Compose, or SSH service definitions.
- bundles: ordered groups of service names.
- gitRepositories: named repository paths.
- selectedGitRepository: active repository for the UI.
- databases: Postgres, MySQL, or SQLite connections.
- logSources: file, SSH, command, journald, or Docker log readers.
- githubTokens: GitHub tokens used for PR, issue, CI, and Actions API access.
- workflows: user-saved git/GitHub workflows built from action, agent, and gate steps.

## Safety Model

NoMoreIDE is designed for safe AI-assisted local development:

- It does not scan or enumerate the whole filesystem.
- It only works from registered services, repositories, databases, and log sources.
- It does not kill external processes that it did not start.
- It reports port conflicts instead of terminating the occupying process.
- Git tools omit hard reset, clean, force push, and branch deletion.
- Git staging requires explicit file paths.
- Database MCP tools are read-only. Writes are staged for the human-only SQL console.
- Database writes require explicit unlock, preview, and commit.
- GitHub merge/comment/create tools require a configured token and explicit user intent.
- SSH services rely on the user's local SSH config and agent.
- Managed service logs are scoped to `.nomoreide/logs/`.

## AI Agent Guide

Recommended session prompt:

```text
Use NoMoreIDE as the shared local workbench for this session. Start by calling nomoreide_list_services and nomoreide_status. Before changing service state, check nomoreide_service_health and recent logs. For Git and GitHub work, inspect status, diffs, PRs, issues, and CI before staging, committing, or merging. For database work, keep MCP queries read-only; stage writes with a sql-write block so the human can review them in the Web UI SQL console.
```

Recommended workflow:

1. Call `nomoreide_list_services`.
2. Call `nomoreide_status`.
3. Use `nomoreide_open_ui` if the human wants the dashboard.
4. For service debugging, call `nomoreide_service_health`, `nomoreide_read_logs`, and `nomoreide_timeline`.
5. For Git work, call status and diffs before staging or committing.
6. For database inspection, list databases, list tables, sample a selected table, then run read-only SQL when needed.
7. For database writes, reply with one `sql-write <connection>` fenced block so the Web UI can stage the statement for human unlock, preview, and commit.
8. For GitHub work, use NoMoreIDE GitHub tools for PRs, issues, comments, CI, and workflow runs.

## Troubleshooting

MCP server does not show up: re-run setup, restart the agent, verify with `/mcp`, and confirm `npx -y nomoreide` works.

Node or npx problems: use Node.js 20 or newer, check `node --version`, or install globally with `npm install -g nomoreide`.

Dashboard port conflict: default port is `4317`; start with `nomoreide web --port=4320`.

Service will not start: check absolute `cwd`, command validity, service health, logs, and timeline.

Logs are empty: confirm the service was started by NoMoreIDE and writes to stdout or stderr.

Git repository is not registered: register an absolute Git worktree path.

Database connection fails: verify the URL, network reachability, and credentials.

## Architecture

Core layer: ConfigStore, ProcessManager, LogStore, GitManager, ServiceHealth, TimelineStore, DbPeek, and ErrorInbox.

MCP server: FastMCP stdio server that registers domain tool groups from `src/mcp/tools/`.

Web server: local HTTP server and route registry under `src/web/routes/`.

React dashboard: Vite, React, Tailwind CSS, feature modules under `src/web/client/src/features/`.

CLI and TUI: `src/index.ts` dispatches to MCP, TUI, web, service, and Git commands.

Data flow:

```text
AI agent -> MCP stdio -> NoMoreIDE tools -> core managers -> managed services, logs, Git, database metadata
Human -> CLI/TUI/Web dashboard -> same core managers
```

## Development

```bash
npm install
npm run dev
npm run dev:web
npm test
npm run build
npm --prefix website run build
```

## Licensing

NoMoreIDE is AGPL-3.0 licensed with a commercial license available. See `LICENSE` and `COMMERCIAL.md`.
